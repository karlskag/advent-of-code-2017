(ns adventofcode.core
  (:gen-class)
  (:require [clojure.test :refer [is]])
  (:import [java.lang Math]))

;TO-USE
; -mapcat
; -cycle
; -repeat
; -range (create infinite seq)
; -update
; -(keep #(get x %)
; -rest

(println "WELCOME TO AOC 2017! :)")
(println "Check out my solutions.")